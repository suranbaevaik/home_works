let userSalaries = {
    Ella: 10000,
    Sophia: 15000,
    Ellie: 20000
}

alert(userSalaries.Ella + userSalaries.Sophia);

let point = {
    a: 5,
    b: 6,
    c: 10
}

alert(point.a + point.b + point.c);

let data = 5 > 10;
let number = data ? '3' : '5';
alert(number);
